include $(APPDIR)/Make.defs

CFLAGS += -I$(CURDIR)

CSRCS = audio_focus_manager.c
MAINSRC = main.c

PROGNAME = $(CONFIG_AUDIOSERVICE_PROGNAME)
PRIORITY = $(CONFIG_AUDIOSERVICE_PRIORITY)
STACKSIZE = $(CONFIG_AUDIOSERVICE_STACKSIZE)
MODULE = $(CONFIG_AUDIOSERVICE)

include $(APPDIR)/Application.mk
