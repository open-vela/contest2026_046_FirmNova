include $(APPDIR)/Make.defs

ifeq ($(CONFIG_RIVOTEK_MAP_SERVICE),y)

CFLAGS += ${INCDIR_PREFIX}$(CURDIR)/include
CFLAGS += ${INCDIR_PREFIX}$(APPDIR)/../contest2026_046_FirmNova/rivotek/services/common/include
CFLAGS += ${INCDIR_PREFIX}$(APPDIR)/../contest2026_046_FirmNova/rivotek/services/wifi_display_service/include

CSRCS += src/rvt_map_api.c

PROGNAME += map_locate
PROGNAME += map_go
PROGNAME += map_show_location
PROGNAME += map_select
PROGNAME += map_dest
PROGNAME += map_route
PROGNAME += map_nav_start
PROGNAME += map_nav_stop
PROGNAME += map_speed
PROGNAME += map_direction

PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)
PRIORITY += $(CONFIG_RIVOTEK_MAP_SERVICE_PRIORITY)

STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)
STACKSIZE += $(CONFIG_RIVOTEK_MAP_SERVICE_STACKSIZE)

MAINSRC += cli/map_locate.c
MAINSRC += cli/map_go.c
MAINSRC += cli/map_show_location.c
MAINSRC += cli/map_select.c
MAINSRC += cli/map_dest.c
MAINSRC += cli/map_route.c
MAINSRC += cli/map_nav_start.c
MAINSRC += cli/map_nav_stop.c
MAINSRC += cli/map_speed.c
MAINSRC += cli/map_direction.c

endif

include $(APPDIR)/Application.mk
