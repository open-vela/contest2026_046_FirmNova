include $(APPDIR)/Make.defs
include $(APPDIR)/../contest2026_046_FirmNova/Common.mk

PROGNAME = $(CONFIG_RIVOTEK_WIFI_BRINGUP_PROGNAME)
PRIORITY = $(CONFIG_RIVOTEK_WIFI_BRINGUP_PRIORITY)
STACKSIZE = $(CONFIG_RIVOTEK_WIFI_BRINGUP_STACKSIZE)
MODULE = $(CONFIG_RIVOTEK_WIFI_BRINGUP)
MAINSRC = wifi_bringup.c

$(eval $(AW_ADD_WIFI_INCLUDES))

include $(APPDIR)/Application.mk
